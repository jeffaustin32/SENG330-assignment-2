All installation instructions are for use with MinGW and may vary if using a different compiler.

Installation instructions:
1. From command line run: "mingw32-make -f MakeFile all clean"
	This will compile all resources and cleanup all intermediary files.
2. Run "main", to execute the application.